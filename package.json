{
  "name": "fify-plugin-marketplace",
  "version": "0.1.2",
  "private": true,
  "description": "Git-backed Codex marketplace for the Fify information UI plugin.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm run validate && npm run smoke"
  }
}
